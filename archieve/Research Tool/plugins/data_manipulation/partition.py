# TODO
# 1. Define complete partition scenarios (domain + source/target + train/test/adaptation).
# 2. Implement reusable helpers for common domain and splitting operations.
# 3. Implement one Pipe variant/strategy for each valid experimental scenario.
# 4. Update DatasetView indices and the ExperimentRecord for each generated partition.
# 5. Later: solve how data-dependent partitions expand into concrete graph nodes.